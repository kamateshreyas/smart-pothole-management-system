{
  "name": "smart-pothole-traffic-system",
  "version": "1.0.0",
  "private": true,
  "description": "Hackathon MVP for pothole reporting, AI-style severity detection, complaints, and traffic alerts.",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:ai": "cd ai-service && python -m uvicorn app:app --reload --port 8000",
    "dev:ai:mac": "cd ai-service && . .venv/bin/activate && python -m uvicorn app:app --reload --port 8000",
    "dev:ai:win": "cd ai-service && .venv\\Scripts\\python -m uvicorn app:app --reload --port 8000",
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}